language: generic

sudo: required

services:
  - docker
  
before_install:
  - docker pull makarius/isabelle:Isabelle2017
  
script:
  - docker run -v kathode/isabelle-theories:/repo makarius/isabelle:Isabelle2017 build -d /repo -l Test
